language: python

python:
  - 2.7

env:
  global:
    - TEST_VAR=reallyAwesomeString

build:
  pre_ci_boot:
    image_name: python
    image_tag: 2.7
    pull: true
  cache: true
  ci:
    - echo "do some things"
    - shippable_cache_container
    - echo "more things"
  on_success:
    - echo "success!"

  on_failure:
    - echo "failed."
